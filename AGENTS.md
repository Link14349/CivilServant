# AGENTS.md

本文件是仓库级开发约束。除非用户在任务中明确覆盖，所有在本仓库工作的 Agent 都必须遵守。

## 1. 开始工作前

在修改产品、架构、模拟规则、Agent 行为或界面前，依次阅读：

1. `README.md`
2. `docs/01-game-design.md`
3. `docs/02-technical-architecture.md`
4. `docs/03-mvp-vertical-slice.md`

不要根据项目名称自行推断产品需求。若实现与文档冲突，应先更新设计决策或向用户说明冲突。

## 2. 当前阶段

- 当前仅有设计文档，没有运行时代码。
- 第一实现目标是“岚州市产业整改”六回合纵向切片。
- 用户明确要求只写文档的任务中，不得顺手创建脚手架、依赖清单或示例代码。
- 不要在纵向切片验证前扩展完整仕途、多城市、多人或常驻 Agent 群。

## 3. 不可违反的产品原则

### 3.1 玩家是领导者，不是超级项目经理

玩家决定目标、优先级、红线、授权方式和重大分歧。具体预算、技术路线、工作清单和日常行政步骤通常由市长、分管领导和部门形成方案。

自由文本解析器不得替玩家擅自补完关键政治选择，也不得要求玩家填写所有实施细节。玩家未明确的操作层事项应交给被授权的 NPC；关键歧义才向玩家追问。

### 3.2 模拟内核是唯一事实来源

- LLM 不得直接修改世界状态。
- LLM 不得决定成功、失败、指标变化或随机事件。
- 所有状态变化必须是通过校验的领域命令和 `DomainEvent`。
- 叙事文本必须从已经结算的事实生成，不能反过来创造事实。

### 3.3 事实、认知和材料必须分离

- 世界事实不能自动进入所有 NPC 上下文。
- NPC 只能访问其身份、渠道和历史允许的信息。
- 玩家看到的是材料和人物观点，不是数据库上帝视角。
- API 必须返回玩家视图 DTO，禁止直接序列化内部实体。

### 3.4 授权与执行偏差是核心玩法

授权对象不同必须能够产生不同方案。执行可以出现拖延、选择性完成、过度执行、数据美化和成本转嫁，但每种偏差都应能追溯到人物动机、能力、资源或信息，而不是随机写作。

### 3.5 承诺和责任不能只存在于对话摘要

以下内容必须结构化保存：

- 条件承诺和人情债；
- 会议、签字和公开讲话形成的责任记录；
- 审计、举报和数据矛盾形成的证据；
- 尚未完成的任务和延迟触发器；
- 关系在具体议题上的变化。

记忆摘要可被重建，不得成为这些状态的唯一存储。

## 4. 技术边界

### 4.1 后端

- 使用 Python 和 FastAPI。
- 使用 `pyproject.toml` 管理 Python 项目与依赖。
- 领域层必须是普通 Python，不依赖 FastAPI、ORM、具体 LLM SDK 或前端类型。
- 公共函数、领域对象和服务必须有类型标注。
- 使用 Pydantic 校验 API 与 LLM 边界；领域内部优先使用明确的值对象和枚举。
- SQLite 用于 MVP，设计上保留迁移到 PostgreSQL 的能力。
- 数据库迁移使用 Alembic，不在应用启动时偷偷改表结构。
- 随机性必须来自注入的带种子随机源，不直接调用全局随机状态。

### 4.2 前端

- 使用 React、TypeScript 和 Vite。
- TypeScript 开启严格模式，不用 `any` 逃避契约设计。
- 组件只展示玩家有权看到的 DTO，不复制模拟规则。
- 游戏提交动作必须带幂等键和会话版本。
- 用户可见文本默认为简体中文，并保留未来本地化边界。
- 交互必须支持键盘操作、可读焦点和基本无障碍语义。
- 在真实界面原型验证前，不引入大型 UI 框架或复杂全局状态库。

### 4.3 LLM Provider

- 具体模型供应商位于适配器之后，应用层不引用供应商专有响应对象。
- Prompt 保存在版本化文件中，不散落在业务代码字符串里。
- 每个 LLM 输出都有明确结构模式、超时和降级路径。
- 对结构错误只进行有限次数修复，不允许无限自我重试。
- 普通页面读取和已结构化选项不得无故调用 LLM。
- Agent 在 MVP 中没有 Shell、文件系统、数据库写入或任意网络工具。

## 5. 计划中的目录责任

```text
backend/src/civilservant/
├── domain/          # 纯领域模型、规则、命令、事件、随机接口
├── application/     # 用例、回合编排、事务和玩家视图组装
├── agents/          # Prompt、结构契约、Provider 适配边界
├── content/         # 场景载入、内容校验、事件触发
├── api/             # FastAPI 路由、DTO、SSE
└── infrastructure/  # ORM、数据库、模型供应商和日志实现
```

依赖方向必须指向领域层：`api` 和 `infrastructure` 可以依赖 `application/domain`，领域层不能反向依赖外部框架。

场景数据放在 `content/scenarios/`，不要把岚州市特有人物或事件条件硬编码进通用模拟内核。

## 6. 编码规范

- 优先小而明确的模块，不建立没有当前用例的抽象层。
- 名称反映领域含义，避免 `Manager`、`Helper`、`Utils` 等模糊容器。
- 金额、比例、时间和指标使用明确单位；金额避免二进制浮点。
- 时间推进只能由回合/领域服务执行，Agent 不得自行修改日期。
- 所有外部 I/O 都应有超时和可诊断错误。
- 不吞异常，不以空列表或空文本伪装失败。
- 日志使用结构化字段，包含游戏、回合、命令和 Prompt 版本标识。
- 不记录 API 密钥、完整敏感配置或不必要的玩家自由文本。
- 注释解释原因和约束，不复述代码。

## 7. 测试要求

任何改变模拟行为的实现都必须包含测试。

### 7.1 必测内容

- 权限和程序判断；
- 注意力和资源消耗；
- 相同种子的确定性；
- 承诺履约、违约和延迟事件；
- NPC 认知隔离；
- 玩家 DTO 不泄漏隐藏事实；
- 重复幂等请求不重复推进回合；
- LLM 失败不会提交部分状态；
- 叙事重试不会重新结算；
- 存档导出和重新载入保持状态一致。

### 7.2 测试隔离

- 默认测试不得访问真实网络或真实模型 API。
- LLM 使用记录好的固定响应或契约 Stub。
- 时间、随机源和 ID 生成器必须可注入。
- 至少保留一个六回合无 LLM 的完整回放测试。

## 8. 内容规范

- 人物、地区、企业和数字均为虚构，并明确声明。
- 不使用真实在任官员作为 NPC，不影射未公开的个人信息。
- NPC 不得只由“忠诚、贪婪、清廉、派系”等单一标签定义。
- 公共利益、职业责任、部门利益、个人前途和风险偏好应能同时存在。
- 违法违规选择应产生制度性后果，不写成现实规避审计、调查或执法的教程。
- 不保证善意必胜，也不把犬儒和腐败设为唯一有效策略。
- 所有重大事件至少包含两个合理但有代价的方向，禁止一个明显正确选项配多个愚蠢选项。

## 9. 文档与决策同步

下列变化必须同步更新文档：

- 玩家职责边界；
- 核心回合循环；
- 领域实体或状态真相模型；
- LLM 与模拟内核的权限边界；
- 技术栈和仓库结构；
- MVP 范围、人物、事件和成功标准。

若实现发现设计不可行，记录原因和取舍，不要让代码静默成为新的产品定义。

## 10. 完成标准

一项实现任务只有在以下条件满足时才算完成：

- 行为符合当前文档和任务范围；
- 测试覆盖新增规则和失败路径；
- 格式化、静态检查和相关测试通过；
- 没有泄漏隐藏事实、密钥或真实个人信息；
- 没有把领域裁决偷偷交给 LLM；
- 用户能从界面或文档理解变化；
- 任何架构或产品变更已同步记录。
